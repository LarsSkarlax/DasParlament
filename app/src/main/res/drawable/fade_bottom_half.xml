<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Transparent at top, solid at bottom.
      If your list background isn't pure white, change endColor to match it (e.g. @android:color/your_bg).
    -->
    <gradient
        android:angle="90"
        android:startColor="#00FFFFFF"
        android:centerColor="#00FFFFFF"
        android:centerY="0.5"
        android:endColor="@color/white" />
</shape>
